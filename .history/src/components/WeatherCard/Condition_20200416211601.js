import React from "react";
import styled from "@emotion/styled";

const Condition = props => {
    const Temp = styled.h1 `
    font-family: "Fira Sans", sans-serif;
    font-size: 2rem;
    font-weight: 200;
  `;

    return (
        <Temp>
        20 degrees
    </Temp> < State className = "clouds" > Clouds < /State>);
};

export default Condition;
import React from "react";
import styled from "@emotion/styled";

const Condition = props => {
    return ( <> <Temp>20 degrees</Temp> < State className = "clouds" > Clouds < /State>
    </ >);
};

export default Condition;
